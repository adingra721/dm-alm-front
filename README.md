# CIMA ALM Frontend

Frontend Angular pour le parametrage ALM assurance.

## Lancement

Demarrer le backend Spring Boot sur `http://localhost:8080`, puis :

```bash
npm install
npm start
```

Application Angular :

```text
http://localhost:4200
```

Les appels `/api/...` sont proxifies vers Spring Boot par `proxy.conf.json`.
